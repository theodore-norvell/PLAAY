{
  "name": "plaay",
  "version": "1.0.0",
  "description": "The plaay web IDE.",
  "main": "mkHTML.js",
  "scripts": {
    "pretest": "tsc",
    "test": "mocha",
    "build": "tsc && browserify -o plaay.js -s main main.js",
    "doc": "typedoc --mode file --excludePrivate --out ../doc/ .",
    "lint": "tslint --project tsconfig.json"
  },
  "author": "Theodore Norvell",
  "license": "Copyright of the developers",
  "devDependencies": {
    "browserify": "^16.2.0"
  },
  "dependencies": {
    "svg.js": "^2.6.4"
  }
}
