{
  "name": "plaay",
  "version": "1.0.0",
  "description": "The plaay web IDE.",
  "main": "mkHTML.js",
  "scripts": {
    "pretest" : "tsc *.ts test/*.ts --module commonjs",
    "test": "mocha",
    "preinstall": "tsc && browserify -o out.js -s mkHTML mkHTML.js",
    "doc": "typedoc --mode file --excludePrivate --out ../doc/ ."
  },
  "author": "",
  "license": "Copyright of the developers"
}
